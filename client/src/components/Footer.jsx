const Footer = () => {
    return (
        <footer className="footer">
            © {new Date().getFullYear()} — Built with <span>&nbsp;Relay</span>
        </footer>
    );
};

export default Footer;
